[package]
name = "usage-tracker"
version = "0.1.0"
edition = "2021"

[lib]
name = "usage_tracker"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sysinfo = "0.32"
toml = "0.8"
dirs = "5"
chrono = { version = "0.4", features = ["serde"] }
